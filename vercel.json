{
    "version": 2,
    "builds":[
        {
            "src":"index.js",
            "use":"@vercel/node",
            "config": { "postinstall": "prisma generate" }
        }
    ],
    "routes":[
       {
        "src":"/(.*)",
        "dest":"index.js"
       }
    ]
}